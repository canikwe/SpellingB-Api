type User {
  id: ID!
  createdAt: String!
  updatedAt: String!
  firstName: String!
  lastName: String!
  email: String!
  # decks: [Deck!]!
}

type Query {
  users: [User!]!
  user(id: Int!): User!
  # decks: [Deck!]!
  # deck(id: Int!): Deck!
  # words: [Word!]!
  # word(id: Int!): Word!
  # deckwords: [DeckWord!]!
  # deckword(id: Int!): DeckWord!
}
